# VALIDATION.md — 사전 등록(pre-registration) 문서

이 문서는 **결과를 보기 전에** 무엇을 예측하고, 어떻게 채점하고, 언제 채택·폐기할지를 고정한다.
바꾸려면 실험 장부에 번호를 붙여 기록하고, 기존 항목은 지우지 않고 취소선으로 남긴다.

작성일: 2026-09-07 · 작성자: Jaeyoung Cho (Claude 보조)

## 0. 정직한 전제 (측정값, SPY 1993-01-29 ~ 2026-09-04, 8,458거래일)

* 상승 비율: 일 54.1% · 20거래일 창 65.4%(겹치지 않는 창 422개 중 64.0%) · 60거래일 창 72.0%(140개 중 70.0%). 2013년 이후는 더 강세: 55.3% / 68~69% / 77~79%.
* 방향(오를지 내릴지)은 사실상 예측 불가: 학계 최고 모형의 월간 out-of-sample R²는 0.5~1%이며 이는 항상-상승 대비 적중률 +0.3%p 수준. 5%p 우위를 α=5%, 검정력 80%로 확인하려면 20일 창 717개(≈57년)가 필요.
* 위험(변동성·낙폭)은 예측 가능: VIX 수준만으로 다음 20일 실현변동성 상위여부 AUC 0.87, ~~20일 내 -5% 낙폭 AUC 0.76~~¹, 다음 20일 상승 여부 AUC 0.52.
  ¹ 0.76(기저율 25.1%)은 창 안 고점→저점 정의에서만 재현; 구현된 `y_dd5_20` 에서는 0.700(홀드아웃 제외)/0.633(2013+)/0.624(2015+), 20일 RV 0.689 — 실험 #1c
* **따라서 이 플랫폼의 주 목표는 "다음 한 달의 위험"이며, 방향 적중률은 항상 기준선 옆에 참고로만 보고한다.**

## 1. 목표변수 (mrl/targets.py 와 동일)

| 이름 | 정의 | 역할 |
|---|---|---|
| `y_dd5_20` | 다음 20거래일 중 종가가 t 종가 대비 -5% 이하로 내려간 적이 있음 | **주 목표** |
| `y_dd10_60` | 다음 60거래일 중 -10% 이하 | 보조 |
| `y_sign_5/20/60` | h거래일 뒤 종가 > 오늘 종가 | 참고(방향) |
| `y_vol_20` | 다음 20일 실현변동성 > 직전 1년 20일-RV 중앙값 | 보조(변동성) |

## 2. 에피소드 정의

SPY 종가 고점 대비 낙폭이 5% / 10% / 20% 이상인 구간. 저점 이후 직전 고점을 회복하면 종료. 회복 전 다시 임계 이상 재하락은 새 에피소드로 계산하되 회복일은 공유.
1993년 이후: ≥10% 12회, ≥5% 36회, ≥20% 4회 (감사 실측). **모든 성적표는 "12회 중 X회" 수준의 넓은 구간을 벗어날 수 없다.**

## 3. 대체 규약 (데이터 한계)

* P3(CNN F&G): 2020-08-03부터만 실이력. 그 이전 구간은 v0 규칙대로 "조회 실패 시 가중치 제외"와 동일하게 제외.
* P6(마감 30분 매수): 1시간봉 730세션(2023-10~)부터만. 그 이전은 제외.
* P7(주도주): 워치리스트 28종목 중 그 시점 65봉 이상 존재하는 종목만. 2013년 22개 → 2026년 27~28개. 생존편향(2026년에 고른 종목)은 제거 불가 — 결과 해석 시 명시. `n_watch_avail` 을 리포트에 표시.
* 6개 신호(P1·P2·P4·P5·P7·P9) 공통 재현 가능 구간: 2015-01-02 ~ (BTC-USD 2014-09 시작, ~~창 504일 확보 후~~ 2y 달력 창(500~507거래일) 확보 후 — 실험 #1b). 이것이 `BACKTEST_START`.
* 창 규칙 보호: 재현 산출물(`results/summary_v0_*.json` 의 `run.window_rule`·`run.signals_v0_sha256`)에 그 재현이 쓴 창 규칙과 `mrl/signals_v0.py` 해시를 기록하고, `run_backtest_v0.py --reuse-replay` 는 이것이 현재 코드와 다르면(또는 기록이 없으면) CSV 를 재사용하지 않고 재현을 다시 돌린다 — 코드 수정이 벤치마크보다 오래 살아남지 못하게(실험 #1b 의 교훈).

## 4. v0 벤치마크 (동결)

* `reference/market_dashboard_v0.py` 의 규칙·임계값·가중치를 그대로 `mrl/signals_v0.py` 로 옮긴다. `tests/test_parity.py` 통과가 "동결 성공"의 정의.
* 두 변형을 모두 보고: `faithful`(라이브와 동일하게 부분 봉 포함) / `completed`(완성 봉만). 두 결과의 차이 자체가 부분 봉 문제의 크기다.
* v0 결과는 이후 모든 실험 리포트 첫 줄에 영구 표기.

## 5. 평가 지표 (가족용 성적표 형식)

1. 톤별 다음 20/60일 적중률 — **항상 "항상-상승" 기준선 옆에**, 겹치지 않는 창 수와 95% 구간(블록 부트스트랩) 병기.
2. 톤별 선행 수익 분포(평균·중앙·10/90분위)와 `y_dd5_20` 비율.
3. 에피소드 표: ≥5%/≥10% 하락마다 첫 경고일, 리드타임, 놓침 여부; 연간 오경보 횟수.
4. 배분 시뮬레이션(톤→비중 100/100/100/50/25%, 비용 5bp): CAGR·MaxDD·최악 월·전환 횟수 vs 보유.
5. (Phase 2부터) Brier 점수와 Brier skill — 기후학(기저율) 대비 **및** VIX 내재 확률 대비.

## 6. 파라미터 예산과 보정 규칙 (Phase 2)

* 독립 리스크 에피소드 ~30개, 겹치지 않는 20일 창 ~150개(2015+) → **적합 파라미터 최대 5개**(요인 로지스틱 계수 + Platt 2개 포함).
* 확장창 walk-forward, 연 1회 재적합, 20거래일 퍼지, 블록 = 18~24개월.
* 홀드아웃: `2024-09-01` 이후는 최종 검증 1회 외 접근 금지.
* 채택 규칙: 홀드아웃 제외 모든 블록에서 Brier skill(vs 기저율) > 0 이고 VIX 내재 확률 대비 skill ≥ 0. 하나라도 위반이면 v0 유지.
* 실험은 번호를 붙여 §8 장부에 기록(변경 내용·사유·결과). 장부에 없는 실험 결과는 채택 불가.
* 각주: 실험 #2 채점은 문자 그대로; 완화안은 #2a(사후); 이 절이 블록을 "18~24개월" 로 적어 둔 결과 24개월 표와 18개월 표가 갈렸으므로,
  배치는 **두 표 모두에서 통과한 단**만 인정한다 — #2d(사후, 소유자 결정 2026-09-08).

## 7. 킬 규칙 (Phase 3)

라이브 장부에 실현 낙폭 에피소드(≥5%) 8회 이상 **또는** 3년 경과 후(늦은 쪽), 블록 부트스트랩 95% 구간으로 평가.
`y_dd5_20` Brier skill 이 0 을 넘지 못하면 대시보드는 "정보 제공 전용"으로 전환(톤·비중 제안 숨김).

## 8. 실험 장부

| # | 날짜 | 변경 | 사유 | 결과 | 채택 |
|---|---|---|---|---|---|
| 0 | 2026-09-07 | v0 동결 (faithful / completed) | 벤치마크 | (Phase 1 리포트) | — |
| 1 | 2026-09-07 | 에피소드 KPI 에 기준선 추가: 경고 시계열 무작위 순환 이동(null, 200회) 탐지율·리드 분포, 임의의 날 오경보율(=1-y_dd5_20 기저율), 새로 켜진 경고만의 리드(median_lead_days_fresh), 탐색 창 상한(lookback=20) 명시·lead_capped 는 창 첫날부터 켜진 경고도 포함 | §5 '항상 기준선 옆에' — 탐지율 100%·중앙 리드 19일이 lookback×경고 비중(46%)의 산물임을 드러내기 위해. KPI 정의(규칙·임계값)는 바꾸지 않음 | null 탐지율 100%·null 중앙 리드 ≈19일 → 실제와 구별 불가; 경고 런 진짜 경보 비중 10.9~13.2% < 기저율 14.7% | 보고 방식만 (v0 규칙 불변) |
| 1b | 2026-09-07 | 창 에뮬레이션을 Yahoo 실제 규칙으로 교정: 504거래일 고정 → (asof-2y, asof] 달력 창(500~507거래일), watch (asof-1y, asof], BTC [last-2y, last] 731행. tests/fixtures/fetch_real_2026-09-07.json 으로 라이브 페이로드 대조 | 라이브 실측(2y = 502행)과 달라 ~0.5% 의 날에 P1·월간 MACD 상태가 뒤집힘 — 동결 벤치마크의 재현 오류 수정 | faithful/completed 재현 재생성 (톤 변경 소수 일) | 수정 (v0 규칙 불변, 데이터 창만) |
| 1c | 2026-09-07 | 목표변수 정의 대조: 감사의 VIX AUC 0.76/0.69·기저율 ~25% 는 창 [t,t+20] 안 고점→저점 -5% 정의(0.760, 기저율 25.1%)에서만 재현; `mrl/targets.py` 의 `y_dd5_20`(t 종가 대비)에서는 0.700(1993~2024-08, n=7,954)/0.633(2013+)/0.624(2015+), 20일 RV20 0.689. §0 문구 정정 | 세 설계·세 심사가 독립 재현. 목표변수는 바꾸지 않음(사후 변경 금지) — 문제는 감사보다 어렵다 | 문서 정정 | 보고 방식만 |
| 2 | 2026-09-07 | Phase 2 사양(ARCHITECTURE_PHASE2.md): 특징 x_vix(B1 로짓)·x_har(GK+OV 고정가중 HAR − ln VIX)·x_ma(Close/SMA180−1); 4-파라미터 L2 로지스틱(C=1.0), 사다리 M0~M3; 학습 1993-10-14~, 1월 첫 거래일 재적합, 20일 퍼지, 첫 재적합 규칙(≥2,000행∧완결 ≥20% 에피소드 → 2003-01-02; 1999 민감도); BLOCKS_24 11개(마지막 20개월)·BLOCKS_18 14개; 벤치마크 B1(드리프트 반사원리)·B1-BGK·B2=M1·기후학(재적합 학습창 평균); 검정 = 40일 블록 부트스트랩 4,000회 + DM-HAC lag 19 + 20 위상 오프셋; 고정 구간 신뢰도·Wilson(n/20); 결정층 r=p/clim (1.5/1.2, 2.5/2.0), 격상 즉시·격하 5세션, 12회/년 KPI·252세션 churn 경보; 소거 M3-PK·M3-HAR96·C∈{0.1,10}; 참조선 v0 Platt(2017~); 보조 출력 HAR OLS(4, 예산 밖)·이벤트 표·귀속; 홀드아웃 하드컷 + unlock 1회 | §6 예산(4/5)·규칙 이행. **사전 관측 공개**: 설계 단계에서 2003~2024-08 OOS 를 이미 봄 — M3 Brier 0.1205, BSS_clim +0.087, BSS_vix +0.202, BSS_bgk +0.102, 24개월 블록 기후학 대비 9/11 >0 (2019-20 −0.038, 2023-24 −0.000), B1 대비 9/11 ≥0 (2007-08 −0.082, 2017-18 −0.029), M1→M3 +13.6×1e-4 (CI −2.8..31.4); 결정층 2003~24: 4.4회/년, 경고 점유 21.7%, 경고 dd5 31.2% vs 정상 11.0% (v0: 83회/년, 46.5%, 18~19% vs 11~12%). 따라서 이 항목 이후 어떤 규칙 변경도 post hoc | `run_calibration.py` / `run_backtest_v1.py` 2026-09-07 (캐시 2026-09-04, 모든 입력 프레임 2024-08-30 하드컷 → 2024-08 마지막 20세션 라벨 NaN): OOS 2003-01-02~2024-08-30 5,453세션·라벨 5,433행(창 271)·기저율 15.4%. **사다리(전체)** M0 Brier 0.1512 (BSS_clim −0.136) · M1 0.1224 (+0.080, vs B1 +0.191) · M2 0.1217 (+0.086) · **M3 0.1210 (BSS_clim +0.091, vs B1 +0.200, vs BGK +0.101, vs M1 +0.012, AUC 0.687)**. **24개월 블록(M3)**: 기후학 대비 >0 **10/11** (실패 #9 2019-20 −0.037; 평균 +0.119), B1 대비 ≥0 **9/11** (실패 #3 2007-08 −0.082, #8 2017-18 −0.029; 평균 +0.239), M1 대비 >0 6/11 (평균 +0.000) → **§6 literal: 실패**(실패 블록 3·8·9; M1·M2 도 같은 블록에서 실패). 단 간 손실차(×1e-4, 40일 블록 CI, DM t HAC19): M0→M1 +288.1 [172.8, 401.3] t 5.40 · M1→M2 +7.1 [−1.3, 15.1] t 1.69 (p 0.091) · M2→M3 +7.2 [−7.8, 22.5] t 0.96 · **M1→M3 +14.3 [−3.0, 32.6] t 1.61 (p 0.108)** · clim→M3 +121.2 [27.9, 231.7] t 2.59 (p 0.010) · B1→M3 +302.4 [190.1, 414.7] t 5.76. 18개월 블록·1999 시작 민감도·소거(M3-PK Brier 0.1208, M3-HAR96 0.1231)·C∈{0.1,1,10} (0.1214/0.1210/0.1209)·신뢰도·Murphy(reliability 0.0016, resolution 0.0112)·시대별 AUC(x_vix 0.72→0.57→0.59)·HAR-RV(OOS log-MAE 0.279, R² 0.49 vs VIX 0.22)·v0 참조선(2017~, n 1,908: Brier 0.1427, BSS_clim −0.010, AUC 0.567) 은 `docs/calibration_p2.html`. **#2a 사후 규칙으로 채점하면** M3 실패(C: M2→M3 하한 −7.8 < 0), M1 은 A∧B∧C 통과 → 톤 모델 후보 M1 (적용하지 않음). **결정층 v1(default, 2003~2024-08)**: 3.9회/년(v0 82.6), 경고 점유 20.6% (caution 16.0% · reduce 4.7%; v0 46.5%), 경고 dd5 26.2%/48.2% vs 정상 11.3% (기저율 15.4%), 진짜 경보 50.0% vs 기저율 15.4% vs v0 12.8%, 5세션 최대 변경 2(상한 3), churn 경보 31세션(최대 14/252); 배분 CAGR 10.2% vs 보유 10.8%, MaxDD −31.0% vs −55.2% (v0 2015~26: 9.9% vs 13.9%, −16.1% vs −33.7%). 민감도 wide 2.4회/년·MaxDD −34.6%, symmetric_dwell 3.7회/년, no_dwell 4.0회/년(churn 경보 67세션) | **literal 실패 → 사전 등록 결과 = 정보 제공 전용(info_only), v0 톤 유지.** `results/model_p2.json` deploy_mode=info_only, tone_model=None; 카드는 확률·상태를 회색 "시험 운용 — 비중 제안 아님" 으로만 표시. 완화안 (b) 는 #2a 에 소유자가 기재하기 전에는 적용하지 않음(`run_calibration.py --acceptance-rule amended` 는 그 기재 뒤에만) |
| 2a | 2026-09-08 (소유자 결정) | §6 완화안 — **사후(post hoc)**: A) 전체 BSS_clim>0 이고 loss-diff 95% 하한>0, 모든 블록 ≥−0.05, ≥8/11 블록 >0; B) B1 대비 전체 ≥0·하한 ≥0·≥8/11 블록 ≥0; C) 정보 단의 하한>0. 톤 모델 = A∧B 와 자기 단의 C 를 만족하는 최상위 단(예상: M1; M3 는 정보 표시), 없으면 정보 제공 전용. 경로 (a) 문자 그대로 → 정보 제공 전용 / (b) 이 완화안 | 블록당 ~25창의 검정력(BSS sd ~0.1). #2 의 사전 관측을 본 뒤의 결정이므로 post hoc 표시 필수 | **경로 (b) 채택** — 소유자 결정 2026-09-08. 2026-09-07 실행에서 M1 이 A∧B∧C 통과(전체 BSS_clim +0.080, clim→M1 하한 +2.1×1e-4, B1→M1 하한 +17.3×1e-4, 블록 >0 10/11·≥0 9/11), M3 는 C 미충족(M2→M3 하한 ≤0) → 톤 모델 = **M1**, M3 는 정보 표시. 이후 실행은 `--acceptance-rule amended`(주간 워크플로 포함), `run.acceptance_rule` 에 기록. **2026-09-08 보정(§8.2 블록 민감도)**: 같은 완화안 규칙을 사전 등록된 18개월 블록 표로 재채점하면 M1·M2·M3 모두 A 를 통과하지 못해(M1 최소 블록 BSS_clim **−0.0996**, 블록 #11 2018-01-01~2019-09-01 n=419; M2 −0.0954, M3 −0.0931) `deploy_mode=info_only` 가 된다 — 즉 **경로 (b) 는 24개월 표에서만 채택된 것**이고 배치 판정은 블록 정의에 의존한다. 이후 모든 실행이 `acceptance.sensitivity_blocks` 로 이 불일치를 기록·표시한다(리포트 머리 태그·판정 상자·정직 스트립). **2026-09-08 두 번째 소유자 결정(#2d)으로 이 칸의 배치는 취소되었다**: 판정은 두 표의 교집합이어야 하므로 완화안 경로 (b) 는 그대로 적용하되 톤 모델은 **없음**(`deploy_mode=info_only`, `tone_model=None`)이다. 아래 #2d 를 함께 읽어야 이 칸이 참이다 | ~~**채택 (post hoc, 24개월 표 한정)**~~ → **규칙만 채택, 배치 없음** (#2d 로 대체 — 18개월 표에서 떨어져 info_only) |
| 2c | 2026-09-08 | 감사 지적 12건 수정(코드·문서, 모델 사양 불변): ① `--holdout-final` 의 unlock 기록을 채점 직후·로그 공개 **직전**으로 옮겨 1회 보장을 원자화 ② 1회 보장의 존재 검사·기록을 정본 `results/holdout_unlock.json` 에 고정(`--results-dir` 로 우회 불가) ③ 장부 라이브 채점을 해제 파일로 게이트 — 해제 전에는 `HOLDOUT_START` 이후 행을 채점·킬룰에서 제외(사유 표시) ④ `model.parameter_band` 상한을 재적합 5 + 라이브 1 = 6 으로 정정(호출자가 넘긴 가장 오래된 재적합이 조용히 잘리던 문제) ⑤ 해제 후 라이브 계수의 결정론 지문을 라이브 학습 구간으로 계산(하드컷 지문은 OOS 기록용으로 병기) ⑥ '2회 적합' 비교를 두 모드 모두 실제 두 번 적합으로 + `> 1e-9` 면 게이트 실패 ⑦ 보정 리포트 ④·차트를 배포 단(M1)의 표로, 표마다 단 표기 ⑧ §8.2 블록 민감도를 같은 규칙으로 재채점해 `acceptance.sensitivity_blocks` 로 공개 ⑨ 손실차 CI 하한을 ×1e-4 로(백분율 반올림이 −1.25e-4 를 '−0.0%' 로 뭉개던 문제), BSS aggregates 를 ±0.000 으로 ⑩ `daily.py` 의 1월 가드 기준점을 '1월 첫 거래일 이후 첫 일요일' 로(1/1 이 금·토·일인 해에 정상 실행에도 죽던 문제) ⑪ 정직 스트립의 amended 판정을 **배치 근거가 된 단**으로 + 'vs M1 은 정의상 0' 을 장부가 확인한 동일성에만 표기 + 출처 혼재 경고 렌더 ⑫ v1 리포트가 `info_only` 에서 '배포 단' 을 주장하지 않도록 | 조용한 실패 금지·1회 보장·라벨과 숫자의 일치 (감사 CONFIRMED 12건) | 테스트 302 → **312 통과**(회귀 테스트 10개 추가). `spec_sha256` 6f786d9f → **81c2a6bf**(model.py 변경) — 계수·CSV 는 불변(`calib_p2_walkforward.csv` sha 59bd2b83…, 두 번 실행 바이트 동일), 재채점 결과·`deploy_mode=tones`/`tone_model=M1` 불변. **공표 수치 변경 1건**: `summary_p2.json.param_band` [0.120904, 0.126262] → **[0.118709, 0.126261]**(④ 의 정정 — 라벨 '최근 5회 재적합+라이브' 가 이제 참) | 수정 (사양·판정 불변) |
| 2d | 2026-09-08 (소유자 2차 결정) | **수용 판정을 사전 등록된 두 블록 표의 교집합으로**: 어떤 단이든 24개월 표 **와** 18개월 표 **모두**에서 적용 규칙(#2a amended)을 통과해야 배치한다. 구현: `calibrate.acceptance(block_scores_by_table, ladder, require_tables=("24","18"))` — 표별 literal/amended 판정을 모두 계산하고 `tone_model` 은 요구 표 전부에서 통과한 단 중 최상위(교집합)로만 정한다(교집합이므로 표 순서 무관). `run_calibration.py --acceptance-blocks {24|18|both}` (기본 **both**), 주간 워크플로도 `--acceptance-rule amended --acceptance-blocks both`. 요구하지 않은 표(1999 시작)는 `acceptance.sensitivity_blocks` 로 계속 공개하고 **자기 사다리 CI 로** 채점한다(다른 표의 CI 를 빌리지 않는다). 기록: `run.acceptance_blocks`·`run.acceptance_require_tables`·`acceptance.per_table`·`acceptance.blocking_tables`·`acceptance.verdict_line` | §6 이 블록을 "18~24개월" 로 사전 등록했으므로 두 표 모두 정당한 사전 등록 표인데 #2a 완화안에서 결론이 갈렸다(24개월 → M1, 18개월 → 없음). 결과를 본 뒤 통과하는 표 하나를 고르는 것은 승자 선택이다 — 둘 다 요구하는 쪽이 사전 등록 문구에 충실하고 더 보수적이다. **#2 의 사전 관측 이후의 규칙 변경이므로 post hoc 이며 그렇게 표시한다**(#2a 와 같은 지위) | 2026-09-08 재실행(`--acceptance-rule amended --acceptance-blocks both`, 캐시 2026-09-04, 하드컷 2024-08-30, OOS 5,453세션·창 271): **24개월 표** M1 통과(A∧B∧C; M2·M3 는 C 미충족) · **18개월 표** M1·M2·M3 모두 **A 미충족** — 최소 블록 BSS 기후학 M1 **−0.0996** / M2 −0.0954 / M3 −0.0931, 셋 다 블록 #11 **2018-01-01~2019-09-01** (허용 하한 −0.05) → **교집합 없음**. 산출: `results/model_p2.json` **deploy_mode=info_only · tone_model=null**, `summary_p2.json.run.acceptance_blocks="both"`, `acceptance.blocking_tables=["18"]`, `acceptance.verdict_line="24개월 표: M1 통과 · 18개월 표: 실패(최소 블록 BSS_clim −0.0996) → 두 표 모두 통과 요구(소유자 결정 2026-09-08) → 배치 없음(정보 제공 전용)"` — 이 한 줄이 카드·보정 리포트 판정 상자·정직 스트립에 그대로 나온다. 판정 밖 민감도(1999 시작 표)는 M1 통과(tones)라 불일치로 공개. `spec_sha256` 81c2a6bf… 불변(코드 변경은 판정 경로뿐). 이 실행에서 Yahoo 전량 재다운로드로 SPY 조정 종가가 움직여 라이브 계수가 최대 6.566e-03 이동 — `determinism.status=data_changed`·`material_drift=true`(게이트 ok, 소유자 검토 대상). 이 이동은 판정을 바꾸지 않는다(18개월 최소 블록은 −0.0996 언저리로, 어느 쪽이든 A 미충족). **재현성 정정(2026-09-08)**: 처음 커밋된 산출물은 13:45:21Z 캐시로 만들어졌는데 3분 뒤 `daily.py` 의 가격 전량 재다운로드가 `data/` 를 13:51:01Z 캐시로 덮어써서 커밋된 트리에서 재현되지 않았다. 현재 캐시(`run.data_sha256=9b125057…`)로 같은 명령을 다시 돌려 `data/`·`results/` 를 한 커밋에 담았고, 이 칸의 수치는 그 **재현된** 값이다(n_pos **1329**, 라이브 계수 x_vix 0.6914557 · x_har 0.5739567 · x_ma −2.3439857 · 절편 −0.9135100 · clim 0.17139541, `calib_p2_walkforward.csv` sha ae03509e…). 배치 결론은 불변(info_only)이므로 소유자 재결정은 필요 없다 — 출처 정정이다. 테스트 312 → **316**(교집합 회귀 4개 추가) 중 **315 통과**. 남은 1건 `test_parity.py::test_window_reproduces_recorded_fetch_real` 은 이 판정 변경과 무관한 **캐시 사건**이다: 같은 날 `daily.py` 갱신으로 SPY 마지막 봉이 2026-09-08 로 올라가면서 2026-09-07(휴장일) ^VIX 유령 행이 `apply_guards`(= SPY 마지막 이후만 제거)를 통과해 캐시 안에 남았고, 2026-09-07 기록 페이로드와 창 행 수가 1행 어긋난다(HEAD 캐시로는 통과). **별도 장부 항목과 소유자 결정이 필요하다 — 여기서 고치지 않았다.** | **채택 (post hoc)** — 배치 없음: `deploy_mode=info_only`, `tone_model=none`. 플랫폼은 확률·리포트·사다리를 정보로만 보여주고 톤·비중을 주장하지 않는다 |
| 2e | 2026-09-08 | 캐시 재구축(daily.py 갱신, 새 거래일 2026-09-08 유입)으로 결정론 게이트가 `data_changed` · `material_drift=true`(계수 최대 이동 6.566e-03, x_ma) 를 보고 — **검토 결과 양성**: 야후 배당조정 반올림 지터로 SPY 상대변화 최대 1.6e-6(^VIX·^GSPC 등 무배당 계열은 비트 동일), 계수 상대 이동 0.28~0.41%(x_vix 0.6915·x_har 0.5740·x_ma −2.3440 기준). 판정(deploy_only/블록 결과)은 불변. 부수 정정: `tests/test_parity.py` 의 유령 봉 보정이 캐시 전진 시 어긋나던 것을 '재구성에 이미 포함됐으면 더하지 않음' 으로 수정(픽스처·창 규칙 불변) | 게이트의 `material_drift` 임계 1e-4 는 **절대차** 기준이라 계수 스케일에 무관 — 스케일이 큰 계수(x_ma ≈ −2.34)는 0.3% 이동에도 경보가 뜬다. 임계 자체는 사전 등록값이라 바꾸지 않고 이 행으로 판정 근거를 남긴다 | 데이터 수정 아님 · 모델 사양·판정 불변 | 기록만 |
| 2d-i | 2026-09-08 (스키마·문서 정정) | `summary_p2.json.run.primary_blocks` → **`run.ladder_blocks`** 로 이름 변경. `--blocks` 는 #2d 이후 §6 판정 표를 고르지 않고 **사다리 표의 블록별 행만** 자른다(판정 표는 `--acceptance-blocks`). 함께: `--blocks` 도움말·`ARCHITECTURE_PHASE2.md` §8.2 제목·§13 CLI 계약 줄 정정, `model_p2.json.extra` 에 `acceptance_blocks`·`acceptance_require_tables` 기록, `--acceptance-blocks 24|18`(표 하나) 실행에 "#2d 를 우회한 민감도 실행 — 배포용 아님" 경고 추가, `daily.py` 는 요구 표가 24·18 두 표에 못 미치는 `acceptance` 를 **배포하지 않는다**(info_only 로 내리고 경고) | `run.primary_blocks` 가 `acceptance.primary_table` 과 모순된 답을 주었고(§6 주 표 주장), 민감도 실행이 정본 `results/` 를 조용히 덮어쓰면 배포로 이어질 수 있었다 — 조용한 실패 금지 | 판정·계수·`spec_sha256` 불변. 산출물 키 이름만 바뀐다(읽는 코드 없음). 함께 고친 표시 오류: 완화안 기준 문자열이 표마다 `≥8/11` 로 고정 출력돼 14블록(18개월)·13블록(1999) 표에서 실제 적용 임계값과 달랐다 — `AMENDED_BLOCK_FRAC=8/11` 은 **비율**이고 표별로 올림해 쓰므로(24개월 8/11 · 18개월 11/14 · 1999 10/13), `_amended` 의 `rule_text` 를 실제 `min_pass`/`n_blocks` 로 렌더하도록 바꿨다(#2a 의 "≥8/11" 은 비율로 읽는다). `docs/calibration_p2.html` 재생성 | 수정 (사양·판정 불변) |
| 2b | (1회) | 홀드아웃 최종 검증 `run_calibration.py --holdout-final`: 2024-09-03~ 를 R_2024/2025/2026 계수로 채점, BSS_clim·BSS_vix·BSS_m1 + 40일 블록 CI, `results/holdout_unlock.json` 생성(재실행 거부) | §6 홀드아웃 1회 규칙. #2/#2a 의 판정을 바꿀 수 없고 Phase 3 킬룰 감시의 첫 행이 된다 | (채움) | — |
| 3 | (Phase 3 예약) | 5번째 슬롯 후보 M4a: 2-상태 가우시안 HMM 필터 P(고변동) on [r_t, ln RV10], θ 1993~2014 동결(적합 파라미터 0), 사다리 단 M3→M4a 로만 채점 | C 실측 AUC 0.650(2015+) > VIX 0.624, θ 동결 시 AUC 변화 <0.001, VIX 판별력 감쇠 보완 | 미실행 | 미정 |
| 4 | (Phase 3 예약) | 후보 M4b: 구리/금 63일 로그 모멘텀 −Δln(HG/GC) (2000-11-29~, 가용성 대체 없이 자기 구간만) | A 실측 +2.0% walk-forward skill(2005+), 그러나 ~20개 후보 중 선택 → 승자의 저주 할인 | 미실행 | 미정 |
| 5 | (Phase 3 예약) | 후보 M4c: SOX/^GSPC 63일 상대수익(1994-08-03~; 라벨 '주도'이지 '폭' 아님) | A 실측 +0.4%(1999+) | 미실행 | 미정 |
| — | 기록 | v0 종합점수 s_t 는 OOS 정보 없음(C 실측 AUC 0.526, skill −0.027; 2026년 선정 워치리스트의 생존편향이 입력에 스며듦; 2026-09-07 재현 2017~2024-08: AUC 0.567, BSS_clim −0.010) → 모델 입력 후보에서 영구 제외, 참조선으로만 | | | |

## 9. 검토했으나 채택하지 않은 방향

계절성(근거 약함·파라미터 낭비), 뉴스 LLM 감성(키 필요·백테스트용 아카이브 없음), 딥러닝 가격 예측(표본 170~420개로는 과적합), 거시 나우캐스팅(월 단위·지연·키 필요).
